# NUTTER-XMD — WhatsApp Bot Management SaaS

## Overview

Full-stack WhatsApp bot management platform. Each user gets exactly one bot. After login, users see a personal dashboard with a hero status banner, a "Status" tab (QR code / pairing code to link WhatsApp), and a "Settings" tab (mode selector + 10 feature toggles). A hidden admin panel is accessible at `?admin=nutterx=true`.

pnpm workspace monorepo using TypeScript.

## Architecture

- **Frontend**: React + Vite (artifact: `nutter-xmd`, previewPath: `/`)
- **API**: Node.js + Express 5 (artifact: `api-server`, port: 8080)
- **Database**: PostgreSQL + Drizzle ORM
- **Auth**: Clerk (frontend + API; `proxyMiddleware` BEFORE body parsers in app.ts)
- **Codegen**: Orval from OpenAPI spec (`lib/api-spec/openapi.yaml` → `lib/api-client-react/src/generated/`, `lib/api-zod/src/generated/`)

## Pages

- `/` — Landing page (public)
- `/sign-in`, `/sign-up` — Clerk auth pages
- `/dashboard` — User dashboard (protected; single-bot-per-user model)
- `/admin` — "Access Restricted" (no query param)
- `/admin?admin=nutterx=true` — Admin control panel login (username: `nutterx`, key: `nutterx2025!`)

## Single-Bot Model

- One bot per user enforced via `UNIQUE` constraint on `userId` in `bots` table
- `getOrCreateBot(userId)` auto-creates on first API call
- API routes: `GET /api/bot`, `PUT /api/bot`, `GET /api/bot/qr`, `POST /api/bot/pair`, `DELETE /api/bot/disconnect`

## Bot Settings

- **mode**: `private` | `public`
- **Feature toggles**: `antiCall`, `antiLink`, `antiSpam`, `welcomeMessage`, `goodbyeMessage`, `autoReply`, `autoRead`, `typingStatus`, `alwaysOnline`, `autoStatus`
- **Other**: `name`, `prefix` (1–5 chars)

## Admin Panel

- Hidden at `?admin=nutterx=true`; token stored in `localStorage["admin_token"]`
- Credentials: username `nutterx`, key `nutterx2025!`
- Routes: `POST /api/admin/login`, `GET /api/admin/bots`, `POST /api/admin/bots/:id/activate`, `POST /api/admin/bots/:id/deactivate`, `GET /api/admin/stats`

## Design

- Dark cyberpunk theme, neon green primary (`#00ff66`)
- Clerk appearance customized to match theme

## WhatsApp / Baileys Integration

- `@whiskeysockets/baileys` — real WhatsApp Web multi-device client
- Sessions stored per user in `artifacts/api-server/sessions/{userId}/` (multi-file auth state)
- `src/lib/whatsapp.ts` — `WhatsAppManager` singleton managing per-user WASocket instances
- QR flow: `GET /api/bot/qr` starts a Baileys session, returns a PNG data URL (`data:image/png;base64,...`)
- Pairing flow: `POST /api/bot/pair` with `{ phoneNumber }` calls `sock.requestPairingCode(phone)` and returns 8-char code
- Disconnect: `POST /api/bot/disconnect` calls `sock.logout()` and deletes session directory
- QR code converted from raw Baileys string to PNG using `qrcode` library
- `protobufjs` and `@hapi/boom` installed as direct API server deps (needed by Baileys at runtime)

## Key Libraries

- `@workspace/db` — Drizzle ORM + schema
- `@workspace/api-zod` — Zod validators generated by Orval
- `@workspace/api-client-react` — React Query hooks generated by Orval
- `@clerk/express` — Clerk middleware for API
- `@clerk/react` — Clerk hooks for frontend
- `@whiskeysockets/baileys` — WhatsApp Web client
- `qrcode` — Converts Baileys QR string to PNG data URL

## Dev Commands

```bash
pnpm --filter @workspace/nutter-xmd run dev      # Frontend
pnpm --filter @workspace/api-server run dev      # API
pnpm --filter @workspace/api-spec run codegen    # Regenerate API clients
pnpm --filter @workspace/db run db:push          # Push DB schema
```
